// Fixture: instância mínima válida do template (v2 schema).
// Existe um único nó obrigatório (ProblemStatement) — qualquer template real terá mais.
{
  "$schema": "../template.schema.json",
  "type": "feature",
  "version": "1.0.0",
  "label": "Mínimo válido",
  "description": "Usado em testes do validador",
  "layout": {
    "kind": "section",
    "padding": 280,
    "background": "{token:section.background}"
  },
  "structure": [
    {
      "id": "problem-statement",
      "component": "ProblemStatement",
      "required": true
    }
  ]
}
